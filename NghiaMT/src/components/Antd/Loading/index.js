import classNames from "classnames/bind";
import styles from "./ButtonCustomer.module.scss";

function LoadingCustomer({ ...props }) {
    return (
        <>ji</>
    );
}

export default LoadingCustomer;